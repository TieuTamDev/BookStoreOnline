<div class="dropdown show">
    <a class="category-nav" href="#" role="button" id="dropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
        <i class="bi bi-list"></i>
    </a>
    <div class="dropdown-menu" aria-labelledby="dropdownMenuLink">
        <a class="dropdown-item" href="@Url.Action("GetAllBook","Category")">Tất cả sản phẩm</a>
        @foreach (var item in ViewBag.CateList)
        {
            <a class="dropdown-item" href="@Url.Action("Index/"+item.CategoryID,"Category")">@item.CategoryName</a>

        }
    </div>
</div>
